const BASE_URL = 'http://localhost:8080';

const API_URLS = {
    BASE_URL: BASE_URL,
    ACCOUNTS: `${BASE_URL}/accounts`,
    BOOKINGS: `${BASE_URL}/bookings`,
    BOOKING_SUMMARY: `${BASE_URL}/booking-summary`,
};

export default API_URLS;